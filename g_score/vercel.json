{
    "version": 2,
    "builds": [{
        "src": "g_score/wsgi.py",
        "use": "@vercel/python"
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "g_score/wsgi.py"
        }
    ]
}